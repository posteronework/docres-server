opencv-python-headless==4.13.0.92
numpy>=2.0,<3
fastapi==0.128.8
uvicorn==0.39.0
python-multipart==0.0.20
einops==0.8.2
safetensors==0.5.3
# torch + torchvision are provided by the base image (pytorch/*, RunPod PyTorch).
# Do NOT pin here to avoid downgrading the image's CUDA-matched build.
# torchvision==0.21.0
